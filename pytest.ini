[pytest]
testpaths = tests
pythonpath = .
addopts = -v --tb=short -ra
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
markers =
    unit: Fast deterministic unit tests
    integration: Multi-component integration tests
    security: SSRF, XSS, and adversarial security tests
